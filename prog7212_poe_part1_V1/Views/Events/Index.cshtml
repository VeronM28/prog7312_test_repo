@model prog7212_poe_part1_V1.Controllers.EventsViewModel

@{
    ViewData["Title"] = "Local Events & Announcements";
}

<div class="modern-events-container">
    <!-- Hero Header Section -->
    <div class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-8">
                    <h1 class="hero-title">Discover Local Events</h1>
                    <p class="hero-subtitle">Explore exciting happenings in your community</p>
                </div>
                <div class="col-lg-4 text-lg-end">
                    <div class="stats-badge">
                        <span class="stats-number">@Model.UpcomingEvents.Count()</span>
                        <span class="stats-label">Upcoming Events</span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container content-wrapper">
        <!-- Search Section -->
        <div class="search-section">
            <div class="search-card">
                <div class="search-header">
                    <i class="fas fa-search"></i>
                    <h5>Find Your Perfect Event</h5>
                </div>
                <div class="search-body">
                    <form method="post" asp-action="Search" asp-controller="Events">
                        <div class="row g-3">
                            <div class="col-md-3">
                                <label class="form-label">Category</label>
                                <select class="form-select modern-select"
                                        name="Category"
                                        id="categorySelect">
                                    <option value="">All Categories</option>
                                    @foreach (var category in Model.Categories)
                                    {
                                        <option value="@category"
                                                selected="@(Model.SearchCriteria.Category == category ? "selected" : null)">
                                            @category
                                        </option>
                                    }
                                </select>
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">From Date</label>
                                <input type="date"
                                       class="form-control modern-input"
                                       name="StartDate"
                                       id="startDate"
                                       value="@(Model.SearchCriteria.StartDate?.ToString("yyyy-MM-dd") ?? "")" />
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">To Date</label>
                                <input type="date"
                                       class="form-control modern-input"
                                       name="EndDate"
                                       id="endDate"
                                       value="@(Model.SearchCriteria.EndDate?.ToString("yyyy-MM-dd") ?? "")" />
                            </div>
                            <div class="col-md-3">
                                <label class="form-label">Keywords</label>
                                <input type="text"
                                       class="form-control modern-input"
                                       name="SearchTerm"
                                       id="searchTerm"
                                       placeholder="Search events..."
                                       value="@(Model.SearchCriteria.SearchTerm ?? "")" />
                            </div>
                        </div>
                        <div class="search-actions">
                            <button type="submit" class="btn btn-modern-primary">
                                <i class="fas fa-search me-2"></i>Search Events
                            </button>
                            <a href="@Url.Action("Index", "Events")" class="btn btn-modern-secondary">
                                <i class="fas fa-redo me-2"></i>Clear Filters
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <!-- Recent Searches -->
        @if (Model.RecentSearches.Any())
        {
            <div class="recent-searches-section">
                <div class="section-header-small">
                    <i class="fas fa-history"></i>
                    <span>Recent Searches</span>
                </div>
                <div class="search-tags">
                    @foreach (var search in Model.RecentSearches)
                    {
                        <span class="search-tag">
                            @if (!string.IsNullOrEmpty(search.Category) && search.Category != "All")
                            {
                                <span class="tag-item"><i class="fas fa-folder"></i> @search.Category</span>
                            }
                            @if (search.StartDate.HasValue)
                            {
                                <span class="tag-item"><i class="fas fa-calendar"></i> @search.StartDate.Value.ToShortDateString()</span>
                            }
                            @if (search.EndDate.HasValue)
                            {
                                <span class="tag-item"><i class="fas fa-calendar"></i> to @search.EndDate.Value.ToShortDateString()</span>
                            }
                            @if (!string.IsNullOrEmpty(search.SearchTerm))
                            {
                                <span class="tag-item"><i class="fas fa-search"></i> "@search.SearchTerm"</span>
                            }
                        </span>
                    }
                </div>
            </div>
        }

        <!-- Recommendations -->
        @if (Model.Recommendations.Any())
        {
            <div class="recommendations-section">
                <div class="section-header">
                    <div class="section-title">
                        <i class="fas fa-star"></i>
                        <h3>Recommended For You</h3>
                    </div>
                    <p class="section-subtitle">Based on your interests and activity</p>
                </div>
                <div class="recommendations-grid">
                    @foreach (var recommendation in Model.Recommendations)
                    {
                        <div class="recommendation-card">
                            <div class="recommendation-badge">Featured</div>
                            <div class="recommendation-content">
                                <h6>@recommendation.Title</h6>
                                <p>@recommendation.Description</p>
                                <div class="recommendation-footer">
                                    <span class="date"><i class="fas fa-calendar-alt"></i> @recommendation.Date.ToString("MMM dd")</span>
                                    <span class="category-badge">@recommendation.Category</span>
                                </div>
                            </div>
                            <a href="@Url.Action("Details", "Events", new { id = recommendation.Id })" class="stretched-link"></a>
                        </div>
                    }
                </div>
            </div>
        }

        <!-- Events Grid -->
        <div class="events-section">
            <div class="section-header">
                <div class="section-title">
                    <i class="fas fa-calendar-check"></i>
                    <h3>Upcoming Events</h3>
                </div>
                <p class="section-subtitle">
                    @Model.UpcomingEvents.Count() event@(Model.UpcomingEvents.Count() != 1 ? "s" : "") available
                    @if (!string.IsNullOrEmpty(Model.SearchCriteria.Category) && Model.SearchCriteria.Category != "All")
                    {
                        <span> in <strong>@Model.SearchCriteria.Category</strong></span>
                    }
                </p>
            </div>

            @if (Model.UpcomingEvents.Any())
            {
                <div class="events-grid">
                    @foreach (var eventItem in Model.UpcomingEvents)
                    {
                        <div class="event-card">
                            <div class="event-card-header">
                                <span class="event-category">@eventItem.Category</span>
                                <span class="event-date">
                                    <i class="fas fa-calendar"></i>
                                    @eventItem.Date.ToString("MMM dd, yyyy")
                                </span>
                            </div>
                            <div class="event-card-body">
                                <h5 class="event-title">@eventItem.Title</h5>
                                <p class="event-description">@eventItem.Description</p>
                                <div class="event-info">
                                    <div class="info-item">
                                        <i class="fas fa-map-marker-alt"></i>
                                        <span>@eventItem.Location</span>
                                    </div>
                                    <div class="info-item">
                                        <i class="fas fa-tag"></i>
                                        <span class="price">
                                            @if (eventItem.Price == 0)
                                            {
                                                <strong>Free</strong>
                                            }
                                            else
                                            {
                                                <strong>@eventItem.Price.ToString("C")</strong>
                                            }
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <div class="event-card-footer">
                                <a href="@Url.Action("Details", "Events", new { id = eventItem.Id })" class="btn-details">
                                    View Details
                                    <i class="fas fa-arrow-right"></i>
                                </a>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="no-events-container">
                    <div class="no-events-icon">
                        <i class="fas fa-calendar-times"></i>
                    </div>
                    <h4>No Events Found</h4>
                    <p>We couldn't find any events matching your criteria. Try adjusting your filters or check back later.</p>
                    <a href="@Url.Action("Index", "Events")" class="btn btn-modern-primary">
                        <i class="fas fa-redo me-2"></i>Reset Filters
                    </a>
                </div>
            }
        </div>
    </div>
</div>

<style>
    :root {
        --primary-color: #6366f1;
        --primary-dark: #4f46e5;
        --primary-light: #818cf8;
        --secondary-color: #8b5cf6;
        --accent-color: #ec4899;
        --success-color: #10b981;
        --warning-color: #f59e0b;
        --bg-color: #f8fafc;
        --card-bg: #ffffff;
        --text-primary: #1e293b;
        --text-secondary: #64748b;
        --border-color: #e2e8f0;
        --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.05);
        --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
        --shadow-lg: 0 10px 25px rgba(0, 0, 0, 0.1);
        --shadow-xl: 0 20px 40px rgba(0, 0, 0, 0.12);
    }

    .modern-events-container {
        background: var(--bg-color);
        min-height: 100vh;
        padding-bottom: 4rem;
    }

    /* Hero Section */
    .hero-section {
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
        color: white;
        padding: 3rem 0;
        margin-bottom: 2rem;
        position: relative;
        overflow: hidden;
        border-radius: 2rem;
        margin-left: 1rem;
        margin-right: 1rem;
    }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><circle cx="50" cy="50" r="2" fill="white" opacity="0.1"/></svg>');
            opacity: 0.3;
        }

    .hero-title {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
        position: relative;
    }

    .hero-subtitle {
        font-size: 1.1rem;
        opacity: 0.95;
        margin-bottom: 0;
        position: relative;
    }

    .stats-badge {
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        padding: 1.5rem;
        border-radius: 1rem;
        text-align: center;
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .stats-number {
        display: block;
        font-size: 2.5rem;
        font-weight: 700;
        line-height: 1;
    }

    .stats-label {
        display: block;
        font-size: 0.9rem;
        opacity: 0.9;
        margin-top: 0.5rem;
    }

    .content-wrapper {
        margin-top: -1rem;
        position: relative;
        z-index: 2;
    }

    /* Search Section */
    .search-section {
        margin-bottom: 2rem;
    }

    .search-card {
        background: var(--card-bg);
        border-radius: 1rem;
        box-shadow: var(--shadow-lg);
        overflow: hidden;
    }

    .search-header {
        background: linear-gradient(135deg, var(--primary-light) 0%, var(--primary-color) 100%);
        color: white;
        padding: 1.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

        .search-header i {
            font-size: 1.25rem;
        }

        .search-header h5 {
            margin: 0;
            font-weight: 600;
            font-size: 1.1rem;
        }

    .search-body {
        padding: 2rem;
    }

    .form-label {
        font-weight: 600;
        color: var(--text-primary);
        font-size: 0.875rem;
        margin-bottom: 0.5rem;
        display: block;
    }

    .modern-input,
    .modern-select {
        border: 2px solid var(--border-color);
        border-radius: 0.5rem;
        padding: 0.625rem 0.875rem;
        transition: all 0.3s ease;
        font-size: 0.95rem;
    }

        .modern-input:focus,
        .modern-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
            outline: none;
        }

    .search-actions {
        display: flex;
        gap: 1rem;
        margin-top: 1.5rem;
        flex-wrap: wrap;
    }

    .btn-modern-primary {
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
        color: white;
        border: none;
        padding: 0.75rem 1.5rem;
        border-radius: 0.5rem;
        font-weight: 600;
        transition: all 0.3s ease;
        box-shadow: var(--shadow-md);
    }

        .btn-modern-primary:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
            color: white;
        }

    .btn-modern-secondary {
        background: white;
        color: var(--text-primary);
        border: 2px solid var(--border-color);
        padding: 0.75rem 1.5rem;
        border-radius: 0.5rem;
        font-weight: 600;
        transition: all 0.3s ease;
    }

        .btn-modern-secondary:hover {
            background: var(--bg-color);
            border-color: var(--primary-color);
            color: var(--primary-color);
        }

    /* Recent Searches */
    .recent-searches-section {
        margin-bottom: 2rem;
    }

    .section-header-small {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--text-secondary);
        font-weight: 600;
        font-size: 0.875rem;
        margin-bottom: 1rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .search-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.75rem;
    }

    .search-tag {
        background: white;
        border: 2px solid var(--border-color);
        border-radius: 2rem;
        padding: 0.5rem 1rem;
        display: inline-flex;
        gap: 0.75rem;
        align-items: center;
        font-size: 0.875rem;
        transition: all 0.2s ease;
    }

        .search-tag:hover {
            border-color: var(--primary-color);
            transform: translateY(-1px);
        }

    .tag-item {
        display: flex;
        align-items: center;
        gap: 0.375rem;
        color: var(--text-primary);
    }

        .tag-item i {
            color: var(--primary-color);
            font-size: 0.75rem;
        }

    /* Section Headers */
    .section-header {
        margin-bottom: 2rem;
    }

    .section-title {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        margin-bottom: 0.5rem;
    }

        .section-title i {
            font-size: 1.5rem;
            color: var(--primary-color);
        }

        .section-title h3 {
            margin: 0;
            font-weight: 700;
            color: var(--text-primary);
            font-size: 1.75rem;
        }

    .section-subtitle {
        color: var(--text-secondary);
        margin: 0;
        font-size: 1rem;
    }

    /* Recommendations */
    .recommendations-section {
        margin-bottom: 3rem;
    }

    .recommendations-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 1.25rem;
    }

    .recommendation-card {
        background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
        border-radius: 1rem;
        padding: 1.5rem;
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
        box-shadow: var(--shadow-sm);
        cursor: pointer;
    }

        .recommendation-card::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 100px;
            height: 100px;
            background: rgba(251, 191, 36, 0.2);
            border-radius: 50%;
            transform: translate(30%, -30%);
        }

        .recommendation-card:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-md);
        }

    .recommendation-badge {
        display: inline-block;
        background: var(--warning-color);
        color: white;
        padding: 0.25rem 0.75rem;
        border-radius: 1rem;
        font-size: 0.75rem;
        font-weight: 600;
        margin-bottom: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .recommendation-content h6 {
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 0.5rem;
        font-size: 1.1rem;
    }

    .recommendation-content p {
        color: var(--text-secondary);
        font-size: 0.875rem;
        margin-bottom: 1rem;
        line-height: 1.5;
    }

    .recommendation-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

        .recommendation-footer .date {
            display: flex;
            align-items: center;
            gap: 0.375rem;
            color: var(--text-secondary);
            font-size: 0.875rem;
            font-weight: 500;
        }

    .category-badge {
        background: white;
        color: var(--text-primary);
        padding: 0.25rem 0.75rem;
        border-radius: 1rem;
        font-size: 0.75rem;
        font-weight: 600;
    }

    .stretched-link::after {
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        z-index: 1;
        content: "";
    }

    /* Events Grid */
    .events-section {
        margin-bottom: 3rem;
    }

    .events-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 1.5rem;
    }

    .event-card {
        background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
        border-radius: 1rem;
        overflow: hidden;
        transition: all 0.3s ease;
        box-shadow: var(--shadow-md);
        border: 1px solid var(--border-color);
        display: flex;
        flex-direction: column;
    }

        .event-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-xl);
            border-color: var(--primary-color);
        }

    .event-card-header {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        padding: 1rem 1.25rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        border-bottom: 1px solid var(--border-color);
    }

    .event-category {
        background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
        color: white;
        padding: 0.375rem 0.875rem;
        border-radius: 1rem;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }

    .event-date {
        color: var(--text-secondary);
        font-size: 0.875rem;
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 0.375rem;
    }

    .event-card-body {
        padding: 1.5rem;
        flex-grow: 1;
    }

    .event-title {
        font-weight: 700;
        color: var(--text-primary);
        margin-bottom: 0.75rem;
        font-size: 1.25rem;
        line-height: 1.3;
    }

    .event-description {
        color: var(--text-secondary);
        font-size: 0.9rem;
        line-height: 1.6;
        margin-bottom: 1rem;
    }

    .event-info {
        display: flex;
        flex-direction: column;
        gap: 0.625rem;
    }

    .info-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--text-secondary);
        font-size: 0.875rem;
    }

        .info-item i {
            color: var(--primary-color);
            width: 16px;
        }

        .info-item .price {
            color: var(--success-color);
            font-weight: 600;
        }

    .event-card-footer {
        padding: 1rem 1.25rem;
        background: var(--bg-color);
        border-top: 1px solid var(--border-color);
    }

    .btn-details {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--primary-color);
        font-weight: 600;
        text-decoration: none;
        transition: all 0.2s ease;
        font-size: 0.9rem;
    }

        .btn-details:hover {
            gap: 0.75rem;
            color: var(--primary-dark);
        }

        .btn-details i {
            transition: transform 0.2s ease;
        }

        .btn-details:hover i {
            transform: translateX(3px);
        }

    /* No Events */
    .no-events-container {
        background: white;
        border-radius: 1rem;
        padding: 4rem 2rem;
        text-align: center;
        box-shadow: var(--shadow-md);
    }

    .no-events-icon {
        font-size: 4rem;
        color: var(--border-color);
        margin-bottom: 1.5rem;
    }

    .no-events-container h4 {
        color: var(--text-primary);
        margin-bottom: 0.75rem;
        font-weight: 700;
    }

    .no-events-container p {
        color: var(--text-secondary);
        margin-bottom: 2rem;
        max-width: 500px;
        margin-left: auto;
        margin-right: auto;
    }

    /* Responsive Design */
    @@media (max-width: 768px) {
        .hero-title

    {
        font-size: 2rem;
    }

    .hero-subtitle {
        font-size: 1rem;
    }

    .stats-badge {
        margin-top: 1rem;
    }

    .search-body {
        padding: 1.5rem;
    }

    .events-grid,
    .recommendations-grid {
        grid-template-columns: 1fr;
    }

    .search-actions {
        flex-direction: column;
    }

    .btn-modern-primary,
    .btn-modern-secondary {
        width: 100%;
    }

    }

    @@media (max-width: 576px) {
        .hero-section

    {
        padding: 2rem 0;
    }

    .section-title h3 {
        font-size: 1.5rem;
    }

    }
</style>